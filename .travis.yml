language: ruby

rvm: 2.3.1

cache:
  bundler: true

addons:
  postgresql: 9.3

env:
  global:
  - DATABASE_URL='postgres://'
  - COVERAGE='1'
  matrix:
  - INTEGRATION_SPECS='1'
  - INTEGRATION_SPECS='0'

before_script:
- psql < db/schema.sql
